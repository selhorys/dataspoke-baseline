# Deployment scope

Use scripts under `helm-charts/bin/` from the repository root. Dev and prod profiles have distinct
contracts; never infer or synthesize production credentials. Consult `spec/feature/HELM_CHART.md`.
