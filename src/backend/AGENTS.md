# Backend scope

Follow `spec/feature/BACKEND.md`, `BACKEND_LLM.md`, and `BACKEND_SCHEMA.md`. Use the
`datahub-api` skill for DataHub-facing work. Backend generators must be followed by an independent
reviewer; add the security reviewer when sensitive paths are touched.
