# Frontend scope

The Next.js UI is a thin client of routes defined in `spec/API.md`; do not invent endpoints or a
streaming surface. Run `pnpm -C src/frontend test` (which includes typecheck) for frontend changes.
