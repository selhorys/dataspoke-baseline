# Workflow scope

Follow the Airflow 3 conventions in `spec/feature/BACKEND.md`. Keep DAGs orchestration-only and put
shared business logic in backend services. Workflow generators require an independent review.
